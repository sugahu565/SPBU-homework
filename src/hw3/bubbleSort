def bubble(lst):
    flag = 1
    n = len(lst)
    while flag == 1:
        flag = 0
        for i in range(1, n):
            if lst[i - 1] > lst[i]:
                lst[i - 1], lst[i] = lst[i], lst[i - 1]
                flag = 1
    return lst

a = list(map(int, input().split()))
print(*bubble(a))